/**
 * classifyProduct.ts — Re-exporta desde el motor unificado
 */
export { classifyProduct, classifyProductText, teachEngine } from './engine';
export type { ClassificationResult } from './engine';
